
  # Dark-Themed Timer and Clock UI

  This is a code bundle for Dark-Themed Timer and Clock UI. The original project is available at https://www.figma.com/design/hVM8YSnqLOUZc8138cdxso/Dark-Themed-Timer-and-Clock-UI.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  